{
    "name": "secops-universal-connector",
    "version": "1.0.0",
    "description": "Universal, extensible middleware between ServiceNow Security Operations and any third-party security tool.",
    "license": "UNLICENSED",
    "imports": {
        "#now:*": "./@types/servicenow/fluent/*/index.js"
    },
    "scripts": {
        "build": "now-sdk build",
        "deploy": "now-sdk install",
        "transform": "now-sdk transform",
        "types": "now-sdk dependencies",
        "tosn-p": "npm version patch --no-git-tag-version && npm run build && npm run deploy",
        "tosn-mi": "npm version minor --no-git-tag-version && npm run build && npm run deploy",
        "tosn-mj": "npm version major --no-git-tag-version && npm run build && npm run deploy",
        "test": "node --experimental-strip-types --test test/*.test.js test/*.test.ts",
        "verify": "npm run build && npm test"
    },
    "devDependencies": {
        "@servicenow/glide": "27.0.5",
        "@servicenow/isomorphic-rollup": "^1.3.10",
        "@servicenow/sdk": "4.12.1",
        "@types/react": "^19.3.0",
        "@types/react-dom": "^19.3.0",
        "typescript": "^5.5.4"
    },
    "dependencies": {
        "react": "^19.3.0",
        "react-aria-components": "^1.21.1",
        "react-dom": "^19.3.0"
    }
}
